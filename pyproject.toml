[tool.ruff]
line-length = 120
target-version = "py311"
src = ["agent_gateway", "mcp_server", "lab", "eval", "fault_injection"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["agent_gateway", "mcp_server"]
