route:
  receiver: agent-gateway
  group_by: ["alertname", "hostname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: agent-gateway
    webhook_configs:
      - url: "http://agent_gateway:8001/webhook/alertmanager"
        send_resolved: true
